# Vercel Postgres 연결 (Vercel 대시보드에서 자동 생성됨)
# Vercel Postgres 생성 시 다음 변수들이 자동으로 설정됩니다:
# - POSTGRES_PRISMA_URL (DATABASE_URL로 사용)
# - POSTGRES_URL_NON_POOLING (필요시 사용)
DATABASE_URL="postgresql://..."

# NextAuth 설정
NEXTAUTH_URL="https://your-domain.vercel.app"
NEXTAUTH_SECRET="your-secret-key-here"
